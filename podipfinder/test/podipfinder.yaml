apiVersion: v1
kind: Pod
metadata:  
  labels:
    run: podipfinder
  name: podipfinder
spec:
  containers:
  - image: jeyaganesh/podipfinder:0.0.1
    imagePullPolicy: Always
    name: podipfinder
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
